# Build-safe defaults — these allow `next build` to complete without errors.
# Real values are injected by Vercel at runtime via environment variables.
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="build_time_placeholder_secret"
GOOGLE_CLIENT_ID="build_time_placeholder"
GOOGLE_CLIENT_SECRET="build_time_placeholder"
